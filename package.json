{
  "name": "zerofactory-dashboard-build",
  "version": "0.0.0",
  "private": true,
  "description": "Build tooling for the Zero Factory dashboard stylesheet. `npm install` then `node dashboard/build_css.mjs` regenerates dashboard/dist/style.css (Tailwind CSS v4) portably — no machine-specific paths.",
  "scripts": {
    "build:css": "node dashboard/build_css.mjs"
  },
  "devDependencies": {
    "tailwindcss": "^4.3.3"
  }
}
