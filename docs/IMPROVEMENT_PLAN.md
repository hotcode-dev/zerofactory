# Zero Factory Improvement Plan

Auto-generated by `zero-factory-improvement-scanner`.
Last scan: 2026-09-11 (UTC+07)

## Board-Registered Projects Scanned

| Board | Git URL | Local Path | Status |
|-------|---------|-----------|--------|
| `zerofactory` | https://github.com/hotcode-dev/zerofactory.git | `~/git/hotcode-dev/zerofactory` | ✅ Scanned |

**Total improvements found:** 1 task created (per the 1-task-per-run cap).
Secondary findings are listed below for future runs.

## Priority Ranking

### 🔴 P0 — CRITICAL (task created this run)

**1. Hardcoded gateway API secret committed to git (security)**
- **Finding:** A real 64-char `API_SERVER_KEY` is hardcoded in 4 tracked `.env` files and committed to git history (commits `67f93ca`, `d001d74`), then pushed to the GitHub remote. It is paired with `API_SERVER_HOST=0.0.0.0` and `GATEWAY_ALLOW_ALL_USERS=true`, so the API is reachable on all interfaces with a known, committed credential.
- **Files (relative):**
  - `profiles/common/.env`
  - `profiles/builder/.env`
  - `profiles/orchestrator/.env`
  - `profiles/reviewer/.env`
  - `profiles/common/.env.example` (already correct — empty placeholder — use as template)
- **Est. impact:** High — full API-server credential exposure. Anyone with the repo can authenticate. Requires key rotation (old key is compromised) + history cleanup.
- **Task:** `zf-21718ce2` — `[SECURITY P0] Rotate & externalize hardcoded API_SERVER_KEY leaking in git` (status: `ready`, assignee: `builder`).
- **Note:** A pre-existing P0 task `zf-9493d068` covers the same leak. The human should consider consolidating these two to avoid duplicated worktrees.

### 🟠 P1 — HIGH (deferred to future runs)

**2. `.gitignore` explicitly unignores concrete `.env` secrets (config/security)**
- `!profiles/*/.env` and `!profiles/common/.env` cause secret files to be tracked. Should track only `.env.example` and ignore the concrete `.env` files.
- **Est. impact:** High — root cause of finding #1; prevents recurrence.

**3. Uncommitted working-tree drift (config)**
- `profiles/common/config.yaml`, `builtin_cron.py`, and `dashboard/dist/index.js` have uncommitted local modifications. Risk of silent divergence between the running gateway and the committed source.
- **Est. impact:** Medium — consistency/reproducibility.

**4. Dispatcher robustness gaps (bug-fix / performance)**
- `profiles/common/plugins/zerofactory-kanban/dispatcher.py`:
  - `repo_path = Path(workspace_path).parent.parent` (line 184) assumes a fixed worktree nesting depth; a different worktree layout breaks repo-root resolution silently (falls back to `os.getcwd()`).
  - `subprocess.run([... "git", "commit" ...], check=True, capture_output=True)` — a no-op commit (nothing staged) raises and is swallowed by the outer `except`, hiding real PR-creation failures.
  - `gh pr view task/<task_id>` is called with a **branch name** but GitHub's `gh pr view` expects a PR number/owner-repo; branch-based lookup can return the wrong PR or fail.
  - `setup_worktree` uses a 5s `timeout` on `git worktree add` — flaky on slow disks/NFS and can corrupt partial worktrees.
- **Est. impact:** Medium — reliability of the automated PR/review loop.

### 🟡 P2 — MEDIUM (backlog)

**5. Missing tests for core engine (testing)**
- `test_plugin.py` (185 lines) does not cover the dispatcher's PR lifecycle, worktree provisioning, or the cron dedupe/env-loading logic added in `builtin_cron.py` (`_load_env_defaults`, `cleanup_duplicate_root_jobs`).
- **Est. impact:** Medium — the most complex logic (dispatch/PR loop) has the least coverage.

**6. `builtin_cron.py` env-loading duplication (refactoring)**
- `_load_env_defaults()` re-scans multiple hardcoded `.env` paths each `ensure_builtin_cron_jobs()` call and rewrites `BUILTIN_CRON_JOBS` model/provider/base_url in place. The list of search paths (incl. a hardcoded `/home/ntsd/hermes.env`) duplicates what the Hermes runtime already resolves.
- **Est. impact:** Low — maintainability.

**7. Committed build artifact (config)**
- `dashboard/dist/index.js` / `dist/style.css` are committed AND modified locally. Build artifacts in the source tree drift from any source of truth.
- **Est. impact:** Low — repo hygiene.

## Notes
- Only board-registered repositories are scanned. No other projects are in scope.
- Per the run's 1-task cap, only the single highest-priority finding (the P0 security leak) was converted into a task this run. The remaining findings are captured here for the next scanner pass.
